import * as React from "react";
import Wrapper from "./{{ properCase name }}.styles";

interface Props {
  children: React.ReactNode;
}

const {{ properCase name }}: React.SFC<Props> = ({
  children,
}) => (
  <Wrapper>
    {children}
  </Wrapper>
);

export default {{ properCase name }};
