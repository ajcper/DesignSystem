import * as React from "react";
import Wrapper from "./{{ properCase name }}.styles";

interface Props {
  children: React.ReactNode;
}

class {{ properCase name }} extends React.Component<Props, {}> {
  public render() {
    return (
      <Wrapper>
        {this.props.children}
      </Wrapper>
    );
  }
}

export default {{ properCase name }};
